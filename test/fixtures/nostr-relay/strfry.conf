db = "./strfry-db/"

relay {
  bind = "0.0.0.0"
  port = 7777
  nofiles = 100000

  info {
    name = "Barrow Alley local interoperability relay"
    description = "Local Nostr signalling fixture for Trystero interoperability tests"
  }

  maxWebsocketPayloadSize = 131072
  autoPingSeconds = 55

  writePolicy {
    plugin = ""
  }
}
